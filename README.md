# Tic Tac Toe Game

A Tic Tac Toe game built with `flutter_bloc` and `freezed`.

- The folder architecture is feature-based, so that it's easier to find what we are looking for.
- There is only one screen (GameScreen).